#include<studio.h>
/**
简单函数:
代码进行重用

函数分类：
库函数和自定义函数
使用库函数需要引入库文件


自定义函数：
形式参数   实际参数
它们分别占用不同的空间，形式参数的修改不会影响是参

传值调用


函数需要先声明在使用
一般的是在.h中声明

每个源文件分别的生成对应的此目标文件，所有
的目标连接在一起形成一个可执行的文件

连接属性：
外部链接
  全局标识符具有外部链接属性，可以在本文件使用，外部文件也可以
  
内部链接
	如果在外部链接上加一个static就可以变为内部
	 
无链接 
	
*/ 
int main(){
	
	return 0;
}
